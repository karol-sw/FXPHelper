from setuptools import setup

setup(
   name='fxphelper',
   version='0.1',
   description='A fixed point arithmetics helper module',
   author='Karol Switala',
   packages=['fxphelper'],
   install_requires=[],
)
