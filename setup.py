from setuptools import setup

setup(
   name='fxphelper',
   version='1.1',
   description='A fixed point arithmetics helper module',
   author='Karol Switala',
   packages=['fxphelper'],
   install_requires=[],
)
